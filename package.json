{
  "name": "shellraining-blog",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "lerna run dev --scope @shellraining/content",
    "build": "lerna run build --scope @shellraining/theme && lerna run build --scope @shellraining/content",
    "preview": "lerna run preview --scope @shellraining/content",
    "test": "lerna run test --scope @shellraining/theme",
    "format": "lerna run format",
    "clean": "lerna exec -- rm -rf dist node_modules/.cache && lerna clean --yes"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@vueuse/core": "^13.6.0",
    "vitepress": "2.0.0-alpha.7",
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "lerna": "^8.2.3",
    "prettier": "^3.6.2"
  }
}
