{
  "name": "shellraining-blog",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "bun run ./tooling/dev.ts",
    "dev:theme": "lerna run dev --scope @shellraining/theme",
    "dev:content": "lerna run dev --scope @shellraining/content",
    "build": "pnpm run check:content && lerna run build --scope @shellraining/theme && lerna run build --scope @shellraining/content",
    "build:theme": "lerna run build --scope @shellraining/theme",
    "build:content": "lerna run build --scope @shellraining/content",
    "preview": "lerna run preview --scope @shellraining/content",
    "test": "lerna run test --scope @shellraining/theme",
    "format": "lerna run format",
    "clean": "lerna exec -- rm -rf dist node_modules/.cache && lerna clean --yes",
    "check:content": "bun run ./tooling/check-content.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@vueuse/core": "^13.9.0",
    "vitepress": "2.0.0-alpha.15",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "lerna": "^8.2.4",
    "mermaid": "^11.12.2",
    "prettier": "^3.7.4"
  }
}
